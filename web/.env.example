# Base URL of the Cloudflare Worker. Public by nature -- it ends up in the
# bundle either way, so it is a repository *variable* (vars.WORKER_URL) in CI,
# not a secret. Leave it empty and the panel builds in public mode only.
VITE_WORKER_URL=https://mcksp-admin.<your-subdomain>.workers.dev
